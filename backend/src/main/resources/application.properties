# Active Profile Configuration
spring.profiles.active=prod

# Available profiles:
# - local: For local MySQL database (recommended for testing)
# - dev: For testing against remote VPS database (when VPS MySQL is configured)
# - prod: For production deployment on VPS